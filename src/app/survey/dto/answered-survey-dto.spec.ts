import { AnsweredSurveyDto } from './answered-survey-dto';

describe('AnsweredSurveyDto', () => {
  it('should create an instance', () => {
    expect(new AnsweredSurveyDto()).toBeTruthy();
  });
});
